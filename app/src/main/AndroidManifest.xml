<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ilhacisneclube">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ilhacisne"
        android:roundIcon="@mipmap/ilhacisne_round"
        android:label="@string/app_nome"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">

        <activity android:name=".login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity"></activity>
        <activity android:name=".login.Cadastrar"></activity>
        <activity android:name=".login.ResetarSenha"></activity>
        <activity android:name=".activity.ExibirRankingTime"></activity>
        <activity android:name=".activity.CadastroCampeonato"
            android:parentActivityName=".activity.MainActivity"></activity>
        <activity android:name=".activity.CadastroTime"></activity>
        <activity android:name=".activity.EditRanking"></activity>
    </application>

</manifest>